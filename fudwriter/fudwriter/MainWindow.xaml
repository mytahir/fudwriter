<Window x:Class="fudwriter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fudwriter"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="White"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="fudwriter" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        >



    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Mode="PrimaryDark" materialDesign:ShadowAssist.ShadowDepth="Depth1" Grid.Column="0" Grid.ColumnSpan="6" Padding="0 -10 0 -10">

            <Menu>
                <MenuItem
                    Uid="0" Click="FileTab_Click"
                    Header="_File" Background="Blue" Foreground="White">
                </MenuItem>
                <MenuItem Uid="1" Click="FileTab_Click"
    Header="_Home">
                </MenuItem>

                <MenuItem Header="_Tools" Uid="2" Click="FileTab_Click">

                </MenuItem>

                <MenuItem Header="_Help">
                    <MenuItem Header="About fudwriter">

                    </MenuItem>
                </MenuItem>

            </Menu>

        </materialDesign:ColorZone>

        <Grid  x:Name="Container" Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Grid.ColumnSpan="3" Margin="0 28 0 0"/>
        <Grid  x:Name="Container2" Grid.Column="0" Grid.Row="0" Grid.RowSpan="1" Grid.ColumnSpan="6" Margin="0 30 0 0"/>

        <Grid Grid.Row="4" Grid.ColumnSpan="6" Height="30" VerticalAlignment="Bottom" Background="LimeGreen">
            <TextBlock Text="Pens :" VerticalAlignment="Center" Foreground="White" Margin="10 0 0 0"/>
            <TextBlock Text="Pdfs :" VerticalAlignment="Center" Foreground="White" Margin="90 0 0 0"/>
        </Grid>
        <TextBlock FontSize="20" FontWeight="Bold" x:Name="textBlock" Grid.Column="1" HorizontalAlignment="Left" Margin="116,47,0,0" Grid.Row="2" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Grid.ColumnSpan="2"/>
    </Grid>

</Window>
